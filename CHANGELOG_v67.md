# v67 — 신규 5편 풀이 100% · 같은 테마 묶음 통합

작성: 2026-08-24 · 개인 프로젝트

두 가지를 처리했다. v66 에서 71.63% 였던 신규 5편의 한국어 풀이를 **100% 로 채웠고**, 읽기 목록에서 같은 테마의 작품이 여러 묶음으로 흩어져 있던 것을 **하나로 합쳤다.**

## 변경 요약

| 지표 | v66 | v67 |
|---|---|---|
| 신규 5편 한국어 풀이 (토큰) | 71.63% | **100.00%** |
| 신규 5편 형태분석 (토큰) | 100% | 100% (유지) |
| 읽기 목록 묶음 수 | 12 | **8** |

버전: `APP_VERSION` v66 → v67 · `CACHE_VERSION` v67 → v68

## 1. 같은 테마 묶음 통합

읽기 목록에 **신약이 4개 묶음으로 흩어져 있었다.** 사복음서+사도행전 · 바울 서신 · 공동서신 · 요한 묵시록. 호메로스도 일리아스와 오뒷세이아가 별개 묶음이었다.

같은 테마는 **하나의 메뉴로 들어가 그 안에서 골라 선택**하도록 바꿨다.

| 통합 후 | 편수 | 하위 구분 |
|---|---|---|
| `gr-nt` 신약성서 | 27 | 사복음서 + 사도행전 5 · 바울 서신 13 · 공동서신 8 · 요한 묵시록 1 |
| `gr-homer` 호메로스 | 6 | 일리아스 5 · 오뒷세이아 1 |

묶음 수가 12 → 8 로 줄어 목록이 한눈에 들어온다.

### 하위 구분은 살렸다

27 편을 한 줄로 늘어놓으면 사복음서와 서신의 구분이 사라진다. `WORK_GROUPS` 에 `subs` 필드를 더하고, `renderGroup` 이 이를 소제목으로 렌더링하도록 했다.

```
subs:[{label:'사복음서 + 사도행전 Εὐαγγέλια', works:[...]}, ...]
```

`subs` 가 없는 묶음은 이전과 똑같이 평면 목록으로 그린다. 기존 묶음은 손대지 않았다.

작품 카드의 `AGDT` 배지도 `분석` 으로 중립화했다 (v64 에서 다른 두 곳은 이미 처리). 이제 분석 출처가 AGDT 만이 아니다.

## 2. 한국어 풀이 100%

풀이가 없던 어형 **846 개 (토큰 1,407 회)** 를 채우기 위해 표제어 **682 개**를 새로 썼다.

| 유형 | 예 |
|---|---|
| 고유명사 | `Στρεψιάδης` 스트렙시아데스 · `Κέβης` 케베스 · `Κυκλάδες` 퀴클라데스 제도 |
| 희극 어휘 | `ἐγκορδυλέω` 둘둘 감고 눕다 · `καταγλώττισμα` 짙은 입맞춤 · `κοππατίας` 코파 낙인 말 |
| 비극 시어 | `πάλλευκος` 새하얀 · `θηκτός` 날 선 · `καλλίνικος` 빛나는 승리의 |
| 역사 술어 | `ἀμιξία` 교류 없음 · `μετανάστασις` 이주 · `σιδηροφορέω` 무기를 지니고 다니다 |
| 선서 술어 | `φθορία` 낙태약 · `ἄρρητος` 말해서는 안 되는 · `ἵστωρ` 증인 |

말놀이가 얽힌 이름은 그 사실을 밝혔다 — `φείδων` "아끼는 이 (이름 말놀이)", `ἵππερος` "말 열병 (말놀이)".

채우는 방식은 v64 와 같다. **`k` 가 비어 있는 항목에만** 넣고, 대상을 신규 5편 본문의 어형으로 한정해 다른 작품의 표시가 바뀌지 않게 했다. 실행 시점에 표제어를 NFC 로 정규화해 저본의 분해형과도 맞춘다.

## 3. 검증

- **브라우저 실측** — 신규 5편 3,423 토큰 전량에 대해 분석 100.00% · 풀이 100.00%, 누락 0
- 작품별 모두 100% — Medea · Nubes · Thucydides · Phaedo · 선서
- 메뉴 실측 — 묶음 8 개, 신약 진입 시 작품 카드 27 개와 하위 소제목 4 개, 호메로스 6 편과 소제목 2 개
- 단어 패널 확인 — `σκάφος` → `의미: 배`
- 사라진 묶음 ID (`gr-iliad` · `gr-nt-gospels` 등) 의 잔여 참조 0 건

## 4. 알려진 한계

- **기존 사전 항목의 분석 정확도는 이번 범위 밖이다.** 이를테면 메데이아 1행의 `σκάφος` 는 문맥상 중성 대격인데 기존 항목이 남성 주격으로 되어 있다. 이 항목은 `src` 가 없는 기존 AGDT 계열 데이터로, 우리 블록은 기존 항목을 덮어쓰지 않는다. 37,000 여 항목의 정확도 감사는 별도 과제다.
- **풀이 682 개는 새로 작성한 것**으로 전문 검토를 거치지 않았다. 다의어는 다섯 작품의 문맥에 맞는 뜻을 앞세웠다.
- **정역 미제공** — 신규 5편 모두.
- 통합 대상은 신약과 호메로스 둘로 한정했다. 비극·희극·플라톤·역사·의학은 이미 테마당 하나여서 손대지 않았다.

## 5. 파일 변경 요약

| 파일 | Δ |
|---|---|
| `data-morph.js` | +24,935 자 · 풀이 보완 블록 신설 (표제어 682 · 대상 846 어형) |
| `index.html` | `APP_VERSION` v66 → v67 · `WORK_GROUPS` 통합 · `renderGroup` 의 `subs` 렌더링 · 배지 문구 |
| `sw.js` | `CACHE_VERSION` v67 → v68 |
| `CHANGELOG_v67.md` | 신규 |

## 6. 다음 세션에서 이어갈 작업

- 신규 5편 정역 (다섯 작품 모두 미제공)
- 커리큘럼 Day 21~60 매핑 (v62 이월)
- 오늘의 단어 카드 (v61 이월)
- Sappho 원문 출처 확보
- 기존 사전 항목의 분석 정확도 감사 (위 §4)
- 잠언집·신규 5편의 `편찬` 분석과 풀이 전문 검토
