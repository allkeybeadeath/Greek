# v65 — 원문 코퍼스 확장 5편

작성: 2026-08-24 · 개인 프로젝트

v61 부터 이월돼 온 **원문 코퍼스 확장** 항목을 이어 처리했다. v63~v64 의 히포크라테스 『잠언집』에 이어, 정전 결손이던 작가 넷을 채우고 의료 윤리의 정전을 더했다. 전집 46 → **51편**.

## 변경 요약

| 신규 작품 | 구간 | 섹션 | 분량 |
|---|---|---|---|
| Euripides *Medea* | 1–100행 (프롤로고스) | 1 | 4,315자 |
| Aristophanes *Nubes* | 1–100행 | 1 | 4,731자 |
| Thucydides *Historiae* | 1권 §1–5 (고고학) | 5 | 4,653자 |
| Plato *Phaedo* | §57–60 | 4 | 6,999자 |
| Hippocrates *Iusiurandum* | 전문 | 1 | 1,610자 |

버전: `APP_VERSION` v64 → v65 · `CACHE_VERSION` v65 → v66

### 메워진 결손

셋째 대비극가 **에우리피데스**, 아티카 **구희극** 전체, 사료 비판의 시조 **투키디데스**, 소크라테스 4부작의 마지막 **파이돈** 이 모두 없던 상태였다. 특히 아리스토파네스는 플라톤 『변론』 §19 가 직접 지목하는 작품이라, 짝 학습이 가능해졌다.

## 1. 원문 확보

**Perseus Digital Library `canonical-greekLit` 의 정본 TEI 를 직접 내려받았다.** 프로젝트의 `align_units.py` 가 이미 같은 저장소에서 파일을 받는 선례를 따랐다.

| 작품 | URN |
|---|---|
| Medea | `tlg0006.tlg003.perseus-grc2` |
| Nubes | `tlg0019.tlg003.perseus-grc2` |
| Historiae | `tlg0003.tlg001.perseus-grc2` |
| Phaedo | `tlg0059.tlg004.perseus-grc2` |
| Iusiurandum | `tlg0627.tlg013.perseus-grc2` |

Diorisis 코퍼스에도 넷이 있었으나 쓰지 않았다. 베타코드 토큰열이라 띄어쓰기·구두점을 추정해 재조립해야 하고, 이는 v63 에서 이미 배제한 방식이다. 정본 TEI 가 있으면 그쪽이 언제나 낫다.

### Sappho 는 제외

v61 계획에 있던 Sappho fr.1/31 은 `canonical-greekLit` 과 `First1KGreek` 양쪽 모두에 `tlg0009` 가 없다. 서정시 장르는 여전히 공백이며, 원문 출처를 새로 찾아야 진척된다.

## 2. 표기

기존 작품의 관행을 그대로 따랐다.

- **운문** (`kind:"verse"`) — `[화자]` 를 자기 줄에 두고 `행번호 본문` 을 잇는다. 아이스킬로스 『페르시아인들』·소포클레스 『안티고네』와 동일
- **산문** (`kind:"prose"`) — 투키디데스는 장 안의 절 번호를 살려 `절번호 본문` 으로, 파이돈·선서는 번호 없이
- **생략부호** — Medea 저본이 쓰는 `’` (U+2019) 를 `ʼ` (U+02BC) 로 통일했다. 기존 코퍼스가 U+02BC 8,530 회 대 U+2019 10 회로 압도적이며, `stripWord` 가 U+2019 만 제거해 조회 키가 어긋나기 때문이다

## 3. 묶음 등록

`WORKS` 추가만으로는 읽기 목록에 나오지 않는다 (v63 §4 의 교훈). `index.html` 의 `WORK_GROUPS` 를 함께 고쳤다.

| 묶음 | 변경 |
|---|---|
| `gr-tragedy` 아티카 비극 | +에우리피데스 → **3대 비극가 완비** (3편 → 4편) |
| `gr-comedy` 아티카 구희극 | **신설** — 희극 묶음이 없었다 |
| `gr-historiog` 역사·전기 | +투키디데스 (3편 → 4편) |
| `gr-plato` 플라톤 대화편 | +파이돈 → **최후 4부작 완비** (3편 → 4편) |
| `gr-medicine` 히포크라테스 의학 | +선서 (2편 → 3편) |

각 묶음의 `author`·`desc` 도 새 구성에 맞게 갱신했다.

## 4. 검증

- JS 스캐너 전수 검사 — 미종료 문자열 0 · 문자열 내 개행 0 · 괄호 잔여 0
- **브라우저 실측** — 전집 51편, 5편 모두 `WORKS` 에 정상 등록, 희극 묶음 노출, 비극 4편 표시 확인
- 렌더링 확인 — 운문 (Medea `[Τροφός]` + 행번호) 과 산문 (선서 전문) 양쪽
- 콘솔 오류는 미리보기 브라우저의 서비스 워커 차단 1종뿐

## 5. 알려진 한계

- **형태분석 커버리지 76.3%** (3,417 토큰 기준). 작품별로 Medea 67.5% · Nubes 64.9% · Thucydides 83.3% · Phaedo 83.6% · 선서 72.1%. 잠언집처럼 100% 로 올리려면 별도 라운드가 필요하다. 비극·희극은 시어가 많아 잠언집보다 품이 더 들 것이다.
- **정역 미제공.** `data-translations.js` 에 다섯 작품 모두 항목이 없다.
- **작품 이미지 없음.** Wikimedia 파일명 추측 금지 원칙 (v54~v55 교훈) 에 따라 검증된 파일을 확보하기 전에는 SVG 폴백을 쓴다.
- 발췌 범위는 v61 계획 그대로다. Medea·Nubes 는 1–100행, Thucydides 는 1권 앞 5장, Phaedo 는 도입 4절뿐이다.

## 6. 파일 변경 요약

| 파일 | Δ |
|---|---|
| `data-works.js` | +24,516 자 · 작품 46 → 51 |
| `index.html` | `APP_VERSION` v64 → v65 · `WORK_GROUPS` 5곳 (희극 묶음 신설 포함) |
| `sw.js` | `CACHE_VERSION` v65 → v66 |
| `CHANGELOG_v65.md` | 신규 |

## 7. 다음 세션에서 이어갈 작업

- 신규 5편의 형태분석 보강 (76.3% → 100%)
- 신규 5편의 정역
- 커리큘럼 Day 21~60 매핑 (v62 이월)
- 오늘의 단어 카드 (v61 이월)
- Sappho 원문 출처 확보 (서정시 장르 공백)
- 잠언집 정역·풀이·`편찬` 분석의 전문 검토
