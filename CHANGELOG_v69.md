# v69 — 오늘의 단어 홈 카드 · 죽어 있던 두 기능 복구

작성: 2026-08-26 · 개인 프로젝트

v61 부터 두 차례 이월된 **오늘의 단어** 를 처리했다. 착수해 보니 뷰는 이미 완성돼 있었고, **참조하는 배열 이름이 존재하지 않아 기능 전체가 죽어 있었다.** 같은 원인으로 SRS 도 함께 멎어 있었다.

## 변경 요약

| 항목 | 이전 | 이후 |
|---|---|---|
| 오늘의 단어 | 항상 "단어 데이터가 없습니다" | 정상 동작 (1,142 단어 풀) |
| SRS 간격 반복 | 오늘 due 0 · 전체 0 | 오늘 due 20 · 전체 1,142 |
| 홈 화면 카드 | 없음 | **신설** |

버전: `APP_VERSION` v68 → v69 · `CACHE_VERSION` v69 → v70

## 1. 원인 — 존재하지 않는 `DEFAULT_W`

`renderWordOfDay()` 는 단어·악센트 분석·전집 용례까지 갖춘 완성된 뷰였다. 그런데 첫 줄이 이렇다.

```js
const all = (typeof DEFAULT_W !== 'undefined' && Array.isArray(DEFAULT_W)) ? DEFAULT_W : [];
```

`DEFAULT_W` 는 v26 이전의 통합 어휘 배열 이름으로, 데이터가 `TEXTBOOK_W` · `DCC_W` · `DAILY_W` · `CIVIC_W` 넷으로 쪼개지면서 사라졌다 (474 줄 주석에 "기존 단어 학습기 DEFAULT_W 배열에서 무손실 이식" 이라 적혀 있다). 그래서 `all` 은 언제나 빈 배열이었고, 화면에는 "단어 데이터가 없습니다" 만 떴다.

**같은 이름을 `srsDueCards()` 도 참조한다.** 그래서 SRS 는 늘 빈 배열을 돌려주었고, 홈의 SRS 카운터 카드는 `due.length` 가 0 이라 아예 렌더링되지 않았으며, SRS 화면은 전체 0 · due 0 을 표시했다.

### 처리

쪼개진 네 배열은 이미 `ALL_VOCAB` 으로 합쳐져 있으므로 별칭을 두어 되살렸다.

```js
const DEFAULT_W = ALL_VOCAB;   // 1,142 단어
```

호출부를 일일이 고치지 않은 이유는, `DEFAULT_W` 를 참조하는 곳이 네 군데이고 모두 "통합 어휘 전체" 를 뜻하기 때문이다. 별칭 하나로 의미가 정확히 맞는다.

## 2. SRS 신규 카드 상한

되살리자 새 문제가 드러났다. `srsDueCards()` 는 아직 학습하지 않은 단어를 모두 `isNew` 로 넣으므로, 첫날 **1,142 개가 한꺼번에 due** 로 잡힌다. 홈 카드가 "1142개 단어가 오늘 due" 라고 알리면 쓸모가 없다.

복습 대상은 전부 내보내되 신규는 하루 상한까지만 넣도록 했다.

```js
const SRS_NEW_PER_DAY = 20;
return review.concat(fresh.slice(0, cap));
```

실측 결과 홈 카드가 "20개 단어가 오늘 due", SRS 화면이 "전체 1142 · 오늘 due 20 · 시작 (20개)" 로 표시된다.

## 3. 홈 카드 신설

이번 이월 항목의 본체다. 커리큘럼 카드와 인용 카드 사이에 두었다.

- 그리스어 표제어를 크게, 그 아래 한국어 뜻·품사 배지·주요 어미를 보인다
- **`renderWordOfDay` 와 같은 시드** (`_seededPick(ALL_VOCAB, _todaySeed())`) 를 써서 홈 카드와 상세 화면이 반드시 같은 단어를 가리킨다
- 카드 전체·버튼·키보드(Enter/Space) 모두 상세 화면으로 진입한다
- 기존 카드들과 같이 `typeof` 가드와 `try/catch` 로 감싸, 어휘 데이터가 없으면 조용히 빈 문자열을 반환한다

시드는 `YYYYMMDD` 라 하루 동안 고정되고, 날짜가 바뀌면 새 단어가 나온다.

## 4. 검증

브라우저 실측 (2026-08-26 기준, 오늘의 단어 `ἔργον`).

- 홈 카드 — `오늘의 단어 / ἔργον / 일, 행위, 작품 / 명사 / -ου, τό / 자세히 →`
- 카드 클릭 → 상세 화면에서 **같은 단어** 확인. 악센트 분석 (`παροξύτονον · 파록시톤`) 과 전집 본문 용례 5 건 (아이스킬로스 『페르시아인들』 등) 정상 표시
- 어휘 탭의 기존 진입 카드도 정상 동작
- SRS 화면 — 전체 1142 · 오늘 due 20 · 학습 중 0
- 콘솔 오류는 미리보기 브라우저의 서비스 워커 차단 1 종뿐

## 5. 알려진 한계

- **`SRS_NEW_PER_DAY = 20` 은 임의로 정한 값이다.** Anki 기본값을 따랐다. 사용자가 조절할 UI 는 없다.
- 오늘의 단어는 **품사·난이도를 가리지 않고** 1,142 개 전체에서 뽑는다. 교재 1과 단어와 DCC 빈도 단어가 같은 확률로 나온다. 학습 단계에 맞춘 가중치는 없다.
- `_seededPick` 은 Mulberry32 로 날짜마다 독립 추출하므로, **드물게 며칠 사이 같은 단어가 다시 나올 수 있다.** 소진형 순환이 아니다.
- 이번에 되살린 SRS 는 v44 에 만들어진 뒤로 실제로 동작한 적이 없다. 알고리즘 자체는 손대지 않았으므로 별도 점검이 필요하다.

## 6. 파일 변경 요약

| 파일 | Δ |
|---|---|
| `index.html` | `DEFAULT_W` 별칭 · `SRS_NEW_PER_DAY` 신설 · `srsDueCards` 상한 · 홈 카드와 핸들러 · `APP_VERSION` v68 → v69 |
| `sw.js` | `CACHE_VERSION` v69 → v70 |
| `CHANGELOG_v69.md` | 신규 |

## 7. 다음 세션에서 이어갈 작업

- 커리큘럼 Day 21~60 매핑 (v62 이월) — 남은 것 중 가장 큼
- SRS 동작 점검 (v44 이후 처음 살아남)
- Sappho 원문 출처 확보
- 기존 사전 항목의 분석 정확도 감사 (v67 §4)
- 정역과 `편찬` 분석, 풀이의 전문 검토
