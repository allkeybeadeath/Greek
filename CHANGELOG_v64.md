# v64 — 잠언집 형태분석·풀이 완전화

작성: 2026-08-24 · 개인 프로젝트

v63 에서 히포크라테스 『잠언집』 본문·형태분석·정역을 실었으나, 형태분석 커버리지가 96.4% 에 그쳤고 단어 패널의 한국어 풀이는 대부분 비어 있었다. 본 라운드는 **둘 다 100% 로 채웠다.**

## 변경 요약

| 지표 | v63 | v64 |
|---|---|---|
| 형태분석 커버리지 (토큰) | 96.4% | **100.00%** |
| 한국어 풀이 (토큰) | 미수록 다수 | **100.00%** |
| `MORPH_LOOKUP` 어형 | 38,425 | 38,572 |
| 원문 교정 누적 | 8건 | **15건** |

버전: `APP_VERSION` v63 → v64 · `CACHE_VERSION` v64 → v65

## 1. 원문 교정 7건 추가

미커버 어형을 하나씩 검토하다 저본의 추가 오식을 찾았다. v63 과 같은 기준 — **독립 전사본 `1st1K-grc1` 이 다른 읽기를 지지하는 경우만** 고쳤다.

| 위치 | 저본 | 교정 | 성격 |
|---|---|---|---|
| 7.60 | `Τοὶσι` | `Τοῖσι` | 악센트 오식 |
| 5.34 | `αἱμοῤῥοϊκαὶ` | `αἱμοῤῥοϊκὰ` | 중성 복수 어미 |
| 6.26 | `γενωνται` | `γίνωνται` | 악센트 누락 (독립 사본 읽기 채택) |
| 3.18 | `διἄγουσι` | `διάγουσι` | 구별부호 오식 |
| 4.37 | `θάνἀτον` | `θάνατον` | 악센트 오식 |
| 3.5 | `νωθροι` | `νωθροὶ` | 악센트 누락 |
| 2.46 | `ἀμαυροι` | `ἀμαυροῖ` | 악센트 누락 |

## 2. 형태분석 100%

### 채우지 못하던 이유

v63 의 미커버 150 어형을 추적한 결과, 단순한 조회 실패가 아니었다.

- **Diorisis 에 토큰은 있으나 `analyses: []`** — Morpheus 가 이오니아 방언형을 분석하지 못한다. `νούσημα` · `τουτέοισι` · `ὁκόταν` 등이 모두 이 경우다.
- 히포크라테스 전집 19편, 헤로도토스 (같은 이오니아 산문) 까지 뒤져도 분석이 없다. **가용 코퍼스 어디에도 기계 분석이 존재하지 않는다.**

### `aph.tagged.tsv` 를 쓰지 않은 이유

같은 본문의 Perseus UD 태그본에는 모든 토큰의 자질이 있다. 그러나 자동 태거 출력이라 오류가 뚜렷했다.

- 표제어를 못 찾으면 **표면형을 그대로 표제어로** 넣는다 (`ῥηΐζει` → `ῥηΐζει`, `ἀποδιδοῦσιν` → `ἀποδιδοῦ`)
- 법 오류 — `κωφωθῇ` 는 부정과거 접속법인데 직설법으로 태깅
- `Tense` 가 Pres/Past/Fut 뿐이라 부정과거·미완료·완료 구분이 뭉개진다

학습기에 틀린 문법을 띄우느니 쓰지 않는 편이 낫다고 판단했다.

### 처리

**147 어형을 직접 분석해 `src:"편찬"` 으로 표시**했다. 나머지는 v63 과 같이 `src:"Diorisis"` 다.

| 출처 | 항목 |
|---|---|
| `Diorisis` | 1,857 |
| `편찬` | 148 |

전집 코퍼스에서 회수한 30 어형도 그대로 쓰지 않고 검토했다. Morpheus 가 `διαλυτικοί` 를 동사 `διαλάω` 로, `κίνησις` 를 형용사로 잘못 분석한 것을 잡아 바로잡았다.

## 3. 한국어 풀이 100%

단어 패널의 `의미:` 는 `MORPH_LOOKUP` 항목의 `k` 필드에서 온다. 잠언집 표제어 876 개 중 기존 사전이 덮는 것은 110 개 (12.6%) 뿐이었다. 합계 1,064 개를 새로 썼다.

- **766 개** — 잠언집 전용 블록의 표제어. 새로 작성
- **298 개** — 기존 사전이 담당하는 어형의 표제어. 새로 작성

기존 항목은 덮어쓰지 않는다. 대신 **`k` 가 비어 있는 경우에만 채우는 보완 패스**를 두었고, 대상을 **잠언집 본문에 나오는 393 어형으로 한정**해 다른 작품의 표시가 바뀌지 않게 했다. 실행 시점에 표제어를 NFC 로 정규화해 저본의 분해형 표기와도 맞춘다.

의학 용어는 통용 병명으로 (`κυνάγχη` → 편도염), 체액설 어휘는 개념을 보존해 옮겼다 (`φλέγμα` → 점액). 이오니아 방언형에는 그 사실을 풀이에 밝혔다 (`νούσημα` → "병 (이오니아형)").

## 4. 검증

- JS 스캐너 전수 검사 — 미종료 문자열 0 · 문자열 내 개행 0 · 괄호 잔여 0
- **브라우저 실측** — 본문 7,077 토큰 전량에 대해 분석 100.00% · 풀이 100.00%, 누락 0
- 단어 패널 확인 — `ταραχῇσι` → `ταραχή / 명사 · 여성 · 복수 · 여격 / 의미: 교란, 뒤틀림`

## 5. 알려진 한계

- **`편찬` 148 항목은 코퍼스 대조로 검증할 수 없다.** 본 프로젝트에서 직접 분석한 것이며, 전문 검토를 거치지 않았다. 출처를 `src` 로 구분해 두었으니 패널에서 바로 식별된다.
- **풀이 1,064 개 전량이 새로 작성한 것**으로, 마찬가지로 검토를 거치지 않았다. 다의어는 잠언집 문맥에 맞는 뜻을 앞세웠다.
- **`Σσπασμοὶ` · `ὑπ χωρήσιες` · `Ἢνὑπὸ` · `ὀξέεες` 등은 그대로 두었다.** 독립 사본도 같게 읽어 교정 근거가 없다. 두 전사본이 같은 계통에서 물려받은 결함으로 보인다. 분석과 풀이에는 이 사실을 밝혀 두었다.
- 정역은 v63 그대로이며 여전히 전문 검토 전이다.

## 6. 파일 변경 요약

| 파일 | Δ |
|---|---|
| `data-morph.js` | +41,701 자 · `MORPH_LOOKUP` 38,425 → 38,572 · 풀이 보완 패스 신설 |
| `data-works.js` | 원문 교정 7건 |
| `index.html` | `APP_VERSION` `'v63'` → `'v64'` |
| `sw.js` | `CACHE_VERSION` `'v64'` → `'v65'` |
| `CHANGELOG_v64.md` | 신규 |

## 7. 다음 세션에서 이어갈 작업

- 잠언집 정역·풀이·`편찬` 분석의 전문 검토
- 커리큘럼 Day 21~60 매핑 (v62 이월)
- 코퍼스 확장 — Euripides *Medea*, Aristophanes *Clouds*, Thucydides 1.1-5, Plato *Phaedo*, Sappho
