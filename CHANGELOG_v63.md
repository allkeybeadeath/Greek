# v63 — 히포크라테스 『잠언집』 전편 추가

작성: 2026-08-18 · 개인 프로젝트

v62 가 초보자 커리큘럼 (Day 1~20) 라운드였고, 본 라운드는 사용자 지시에 따라 **원문 코퍼스 확장** 트랙에서 히포크라테스 *Ἀφορισμοί* (잠언집) 전 7권을 `WORKS` 에 추가했다. v61 부터 이월돼 온 코퍼스 확장 항목 중 첫 결실이다.

## 변경 요약

- 신규 작품 1편 — `hippocrates-aphorisms` (전집 45 → **46편**)
- 신규 본문 — 7 섹션 · 413 항 · 48,993 자
- 원문 교정 8 건 (독립 사본 대조로 확인된 기계적 오식만)
- 버전: `APP_VERSION` v62 → v63 · `CACHE_VERSION` v63 → v64

## 1. 원문 출처와 확보 경로

**저본**: Perseus Digital Library, `tlg0627.tlg012.perseus-grc2` (Émile Littré, *Oeuvres complètes d'Hippocrate*, Amsterdam: Hakkert 1961 재판 기준). CC BY-SA 3.0.

로컬에 이미 내려받아 둔 TEI XML 을 직접 파싱했다. 그리스어 원문을 기억이나 추정으로 입력한 부분은 **없다**. 검토한 후보 소스는 셋이었다.

| 소스 | 판정 |
|---|---|
| `tlg0627.tlg012.perseus-grc2.xml` (TEI) | **채택.** 잠언 단위 `div` 구조, 정본 |
| `aph.tagged.tsv` | 위 파일에서 파생된 토큰열. 재조립 시 공백·구두점 추정 필요 → 부채택 |
| `Diorisis .../Aphorisms (012).json` | 베타코드 저장, `fracture_tokens.tsv` 가 지적한 결함 27 건 보유 → 부채택 |

### TEI 파싱에서 걸러낸 구조 결함

Perseus 파일의 `7.18` div 가 이후 잠언 전체 (`18b`, `19`, `20` …) 를 **중첩으로 품고 있다**. 단순히 `itertext()` 로 읽으면 7.18 항에 뒤따르는 본문이 모두 흡수돼 총량이 56,288 자로 부풀었다. 직속 자식 `<p>` 만 읽고 하위 `div` 를 제외하도록 고쳐 47,107 자로 정상화 (독립 경로인 TSV 재조립본 47,099 자와 대조 확인).

## 2. 구조

`WORKS` 스키마 그대로. 섹션은 **권 단위 7개**, 항 번호는 신약 (`nt-*`) 의 절 표기 관행을 따라 `번호 본문` 을 이스케이프 개행으로 연결했다.

| 섹션 | 항 수 | 길이 |
|---|---|---|
| 1권 | 25 | 5,091 자 |
| 2권 | 54 | 5,827 자 |
| 3권 | 31 | 5,975 자 |
| 4권 | 83 | 9,049 자 |
| 5권 | 72 | 9,268 자 |
| 6권 | 60 | 5,316 자 |
| 7권 | 88 | 8,467 자 |

권별 항 수는 표준 Littré 번호 (25·54·31·83·72·60·87) 와 일치하며, 7권은 사본 전통상 분리 항인 `18b` · `59b` 를 별도 항으로 두어 88 이 된다. Perseus 가 79·80 을 병합 표기한 `79_80` 은 표시상 `79–80` 으로 다듬었다.

유니코드는 기존 파일 관행에 맞춰 NFC 로 정규화했다 (분리돼 있던 결합 거센기식 `ρ` + U+0314 107 건이 `ῥ` 로 합성).

## 3. 원문 교정 8 건

교정은 **독립 전사본** `tlg0627.tlg012.1st1K-grc1.xml` 의 어형 사전과 대조해, 저본에만 있고 독립 사본이 다른 읽기를 지지하는 경우로 한정했다. 모두 1 글자 수준의 기계적 오식이다.

| 위치 | 저본 | 교정 | 성격 |
|---|---|---|---|
| 1.2 | `τα ραχῇσι` | `ταραχῇσι` | 어중 공백 |
| 1.5 | `ὁγιαίνουσι` | `ὑγιαίνουσι` | ὁ / ὑ |
| 2.6 | `νοσέεει` | `νοσέει` | ε 중복 |
| 4.17 | `ἐκτικρούμενον` | `ἐκπικρούμενον` | τ / π |
| 5.15 | `μεθίσταντα` | `μεθίστανται` | ι 탈락 |
| 6.40 | `φλεγμοῆς` | `φλεγμονῆς` | ν 탈락 |
| 7.68 | `κλείω` | `πλείω` | κ / π |
| 7.81 | `νοῦσοξ` | `νοῦσος` | ξ / ς |

### 교정하지 않은 8 건

검사에서 함께 걸렸으나 **독립 사본 쪽이 오류**라 저본을 유지했다. 대조 없이 "사전에 없다" 만으로 고쳤다면 오히려 훼손됐을 항목들이다.

`ἀτρεμέουσιν` (1st1K: `ἀτρεμένουσιν`) · `ἀπογηράσκουσιν` (`ἀπογηράκουσιν`) · `λεπτὰ` (`λεττὰ`) · `οὔλων` (`οὔλω ν`) · `ἀμβλυωπίαι` (`ἐμβλυωπίαι`) · `ἐφημέρων` (`ἐρημέρων`) · `παχυνθῆναι` (`παχυνῆναι`) · `φλέβας` (`φλέβασ`, 어말 시그마 표기)

## 4. 등록부 연결 — `WORKS` 추가만으로는 노출되지 않는다

`data-works.js` 에 항목을 넣는 것만으로는 **읽기 화면에 나타나지 않는다**. 브라우저 실측에서 발견했다. `index.html` 의 작품 묶음 등록부에 id 를 넣어야 목록에 잡힌다.

| 위치 | 변경 |
|---|---|
| `WORK_GROUPS` 의 `gr-medicine` | `works` 배열에 `'hippocrates-aphorisms'` 추가 (묶음이 1편 → **2편**, 0/7 → **0/14 섹션**) |
| 같은 항목의 `desc` | 역병론 단독 설명 → 잠언집 포함 설명으로 갱신 |
| 작품 ID → 이미지 매핑 | `'hippocrates-aphorisms': 'hippocrates-bust-aph'` 추가 |
| 이미지 정의 | `hippocrates-bust-aph` 신설 — **파일명은 검증된 기존 항목 그대로 재사용**, 설명만 *Ἐπιδημίαι* → *Ἀφορισμοί* 로 교체 |

마지막 항목은 초상을 공유하면서 설명문이 역병론을 가리키는 어긋남이 상세 화면에 그대로 노출돼 분리한 것이다. Wikimedia 파일명 추측 금지 원칙 (v54~v55 교훈) 에 따라 새 파일명을 만들지 않고 기존 검증 파일을 재사용했다.

아이콘 매핑은 추가하지 않았다 — 기본 폴백 `📜` 가 격언집에 적절하고, 역병론의 `⚕️` 와도 구분된다.

## 5. 검증

- JS 문자열 상태를 추적하는 스캐너로 전수 검사 — 문자열 리터럴 2,024 개 모두 정상 종료, **문자열 내부 실제 개행 0**, 괄호 잔여 `{}` `[]` `()` 모두 0
- 항 수 413 (권별 25·54·31·83·72·60·88)
- 그리스 문자 비율 0.9989
- 교정 8 건 반영 확인 · 교정 전 오식 잔존 0
- 작품 수 45 → 46
- **브라우저 실측** (로컬 http 서버 + 실제 JS 엔진) — `WORKS.length` 46, 잠언집 413 항 파싱, 스키마 키가 `plato-apology` 와 완전 일치, 읽기 목록 → 묶음 → 작품 → 1권 본문까지 렌더링 확인. 교정된 `ταραχῇσι` 가 화면에 반영된 것도 확인
- 콘솔 오류는 서비스 워커 등록 실패 1종뿐 — 미리보기 브라우저가 SW 를 차단해서 나는 것으로 본 변경과 무관 (일반 브라우저·HTTPS 에서는 발생하지 않음)

> 작업 중 한 차례, 스크립트 생성 단계에서 이스케이프가 무력화돼 JS 문자열 안에 실제 개행이 들어간 파일을 썼다. 스캐너가 아니라 `repr` 출력에서 발견했고 백업에서 되돌린 뒤 재생성했다. 위 스캐너 검사는 이 사고 이후 도입한 것이다.

## 6. 형태분석 보강 — 커버리지 63.1% → 96.4%

본문만 넣으면 단어 탭 조회가 절반 남짓만 작동한다. 이오니아 방언형이 `MORPH_LOOKUP` 에 없기 때문이다. **1,417 어형**을 추가해 해결했다.

### 소스 선택

| 후보 | 판정 |
|---|---|
| `aph.tagged.tsv` 의 UD 자질 | **부채택.** `Tense` 가 Pres/Past/Fut 뿐이라 부정과거·미완료·완료 구분이 뭉개진다. 학습기에 틀린 시제를 표시할 위험 |
| Diorisis JSON 의 `analyses` | **채택.** Morpheus 형식 (`pres ind act 3rd sg`) 이라 앱의 표기 규약과 1:1 대응 |

베타코드 변환은 `align_units.py` 의 `beta_to_unicode` 를 그대로 재사용했다 (Perseus 정렬에서 99.68% 로 검증된 함수).

### 표기 규약

기존 데이터에서 실제 사용 빈도를 세어 우세한 쪽을 따랐다.

| 항목 | 채택 | 근거 |
|---|---|---|
| 부정사 | `동사 · 시제 · 부정사 · 태` | 1,130회 (vs `부정사 · 시제 · 태` 625회) |
| 중간수동 | `중간/수동` | 1,611회 (vs `중수동` 10회) |
| 비교급 | 격 뒤에 배치 | `형용사 · 중성 · 단수 · 대격 · 비교급` |

비일의(非一意) 어형은 슬래시를 유지했다 (`남성/중성`, `주격/호격/대격`). 조합을 전개하면 항목이 폭증하고, 실제로 그 형태가 중의적이라는 정보가 사라진다.

### 병합 방식

파일 끝의 PROIEL NT 블록이 쓰는 패턴을 그대로 따라, IIFE 안에서 `if(!(k in MORPH_LOOKUP))` 가드로 병합한다. **기존 AGDT 항목은 절대 덮어쓰지 않는다.** 각 항목에 `src:"Diorisis"` 를 달았다.

### 부수 수정 — 출처 표기

- 단어 패널이 모든 분석을 `AGDT 수동 분석` 으로 표기하고 있었다. 항목의 `src` 를 읽어 출처명을 만들도록 고쳤다 (AGDT / PROIEL / Diorisis). 기존 `nt-john` 도 PROIEL 출처인데 AGDT 로 표시되던 오류가 함께 해소된다.
- 작품 화면의 `AGDT 분석 제공` 배지도 `형태분석 제공` 으로 중립화하고, `MORPH_WORKS` 에 `hippocrates-aphorisms` 를 등록했다.

### 남은 150 어형

`κενεαγγείη` · `αὐτέῳ` · `ὁκόταν` · `τουτέοισιν` 등 판본 고유의 이오니아 형이 Diorisis 에 다른 철자로 실려 매칭되지 않는다. 생략형 4건 (`οὐδʼ` 등) 은 아포스트로피 표기 차이 (U+02BC vs U+0027) 를 정규화해 회수했다.

## 7. 정역 — 전 7권 575 문장

`data-translations.js` 의 `WORK_TRANSLATIONS['workId:secN']` 은 **앱이 계산하는 문장 인덱스와 1:1 로 정합해야 한다.** 임의로 잠언 단위로 끊으면 어긋난다.

그래서 `renderWorkSection` 의 분할 로직 (종결부호 `. ; · ? !` 로 끝나는 토큰에서 인덱스 증가, 줄바꿈은 증가시키지 않음) 을 파이썬으로 재현해 문장 목록을 뽑았다. 브라우저 실측에서 1권 `.sent` 인덱스가 **61 개로 계산값과 정확히 일치**함을 확인했다.

| 권 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 계 |
|---|---|---|---|---|---|---|---|---|
| 문장 | 61 | 72 | 44 | 105 | 110 | 69 | 114 | **575** |

### 종결부호 8건 보완

분할에 직접 영향을 주는 문제가 있었다. 저본에서 종결부호가 빠진 잠언 11건이 다음 잠언과 한 문장으로 붙어 버린다 (예: 1권 21항 끝 `χωρίων` 뒤에 곧바로 `22` 가 이어져 `χωρίων22` 가 된다). 독립 사본이 마침표를 지지하는 **8건만** 보완했다.

`1.21` · `2.50` · `3.27` · `4.12` · `7.10` · `7.24` · `7.41` · `7.51`

`4.2` · `7.44` · `7.57` 은 독립 사본에도 종결부호가 없어 그대로 두었다. 이 셋은 다음 잠언과 한 문장 단위로 묶여 표시되며, 번역도 그 단위로 붙였다.

### 번역 기조

파일 상단에 명시된 기존 방침 (직역 우선, 학습용) 을 따랐다. 의학 용어는 통용 병명으로 옮기되 (`κυνάγχη` → 편도염, `εἰλεός` → 창자막힘), 체액설 어휘는 개념을 바꾸지 않고 그대로 살렸다 (`χολή` → 담즙, `φλέγμα` → 점액, `κένωσις` → 비움).

## 8. 알려진 한계

- **번역은 학습용 초역이다.** 575 문장 전량을 옮겼으나 전문 검토를 거치지 않았다. 특히 5권의 부인과 항목과 4권의 소변 진단 항목은 술어가 까다로워 재검토 가치가 있다.
- **MORPH 미커버 150 어형** (커버리지 96.4%). 위 §6 참조.
- **`ὑπ χωρήσιες` (5.64)** — `ὑποχωρήσιες` 의 어중 공백 오류로 보이나, **독립 사본도 같은 형태로 읽어** 교정 근거가 없어 그대로 두었다. 두 전사본이 같은 계통에서 물려받은 결함으로 추정된다. 어형 사전 대조 방식의 한계 사례이기도 하다 — `ὑπ` 과 `χωρήσιες` 가 각각 실재 어형이라 미등재 검사에 걸리지 않았다.
- **판본 이문 미표기.** Littré 판 단일 계통만 실었고 이문 장치는 없다.

## 9. 파일 변경 요약

| 파일 | Δ |
|---|---|
| `data-works.js` | +49,788 자 · 작품 45 → 46 · 종결부호 8건 보완 |
| `data-morph.js` | +132,937 자 · `MORPH_LOOKUP` 37,008 → 38,425 어형 · `MORPH_WORKS` 등록 |
| `data-translations.js` | +27,621 자 · `WORK_TRANSLATIONS` 7 키 · 575 문장 |
| `index.html` | `APP_VERSION` `'v62'` → `'v63'` · 묶음 등록부·이미지 매핑·이미지 정의 (+115 자) |
| `sw.js` | `CACHE_VERSION` `'v63'` → `'v64'` (1 line) |
| `CHANGELOG_v63.md` | 신규 |

> `APP_VERSION` 과 `CACHE_VERSION` 의 한 단계 어긋남 (v63 / v64) 은 본 라운드 이전부터 있던 상태이며, 그대로 각각 한 단계씩 올렸다.

## 10. 다음 세션에서 이어갈 작업

`NEXT_TASKS.md` 가 **v60 기준으로 낡아 있다** (v61 정리 라운드, v62 커리큘럼 반영 안 됨). 갱신 필요.

- 잠언집 번역 전문 검토 (위 §8)
- v61 이월 잔여 코퍼스: Euripides *Medea*, Aristophanes *Clouds*, Thucydides 1.1-5, Plato *Phaedo*, Sappho fr.1/31
- v62 본 트랙 잔여: 커리큘럼 Day 21~60 매핑
