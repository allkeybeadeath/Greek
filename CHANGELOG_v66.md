# v66 — 신규 5편 형태분석 100% · 줄표 토큰화 수정

작성: 2026-08-24 · 개인 프로젝트

v65 에서 실은 다섯 작품의 형태분석 커버리지가 76.3% 였다. 이를 **100% 로 채웠고**, 그 과정에서 앱 전체에 걸친 토큰화 결함을 하나 찾아 고쳤다.

## 변경 요약

| 지표 | v65 | v66 |
|---|---|---|
| 신규 5편 형태분석 (토큰) | 76.3% | **100.00%** |
| 신규 5편 한국어 풀이 (토큰) | 0% | 71.63% |
| `MORPH_LOOKUP` 어형 | 38,572 | 39,256 |

버전: `APP_VERSION` v65 → v66 · `CACHE_VERSION` v66 → v67

작품별 최종: Medea 100% · Nubes 100% · Thucydides 100% · Phaedo 100% · 선서 100%

## 1. 줄표 토큰화 수정 — 앱 전체 결함

`stripWord` 는 줄표 (`—` `–`) 를 **지운다.** 그런데 토큰 분리는 공백으로만 하므로, `γʼ—οὐ` 같은 표기가 `γʼοὐ` 라는 한 덩어리 조회 키가 되어 버린다. 사전에 있을 수 없는 형태다.

신규 5편에서 6건이 걸려 원인을 찾다가, **전집 전체에 146 종**이 있음을 확인했다.

| 작품 | 병합 토큰 |
|---|---|
| `plato-apology` | 48 |
| `plato-euthyphro` | 14 |
| `nt-2corinthians` | 13 |
| `plato-crito` | 8 |
| 그 밖 | 63 |

절반 이상 (79종) 이 조회 불가 상태였다. 이를테면 『변론』의 `ἀλήθειαν—οὐ` 는 `ἀλήθειανοὐ` 가 되어 단어를 눌러도 아무것도 나오지 않았다.

### 처리

`renderWorkSection` 과 `renderPassage` 의 토큰 분리에 줄표를 분리자로 더했다.

```
line.split(/(\s+)/)  →  line.split(/(\s+|[—–])/)
```

줄표 자체는 `stripWord` 가 빈 문자열을 돌려주므로 단어로 감싸이지 않고 **화면에는 그대로 표시된다.** 브라우저 실측으로 확인 — `ἀλήθειαν` 이 개별 토큰이 되고, 병합 토큰은 0 이 되며, 본문의 줄표는 유지된다.

전집 전체 형태분석 커버리지가 93.99% → 94.02% 로 올랐다. 폭은 작지만, 걸려 있던 토큰이 하필 플라톤 대화편에 몰려 있어 체감 효과는 그보다 크다.

## 2. 형태분석 채우기

### Diorisis 전체 코퍼스 수집

잠언집 때와 달리 **Medea 가 Diorisis 에 없다** (에우리피데스 13편 중 미수록). 단일 작품 파일로는 해결되지 않아, **전체 코퍼스 821편 (2.0 GB) 을 훑어 필요한 어형만 골라내는** 방식을 썼다. 미커버 685 어형 중 **625 개를 회수**했다.

### 직접 분석 59 건

나머지는 성격이 갈렸다.

| 유형 | 수 | 예 |
|---|---|---|
| 시어·희귀어 | 21 | `δρασείουσαν` · `ταυρουμένην` · `ξυνωρικεύεται` · `τᾠκίδιον` |
| 생략형 (어말 `ʼ`) | 19 | `ἐκπλαγεῖσʼ` · `προδοῦσʼ` · `ἤκουσʼ` |
| 화자·고유명사 | 11 | `Τροφός` · `Παιδαγωγός` · `ΕΧ` · `ΦΑΙΔ` |
| 선서의 이오니아형 | 5 | `μαθητῇσι` · `ἐπιορκέοντι` · `ἀνδρῴων` |
| 저본 결함 | 3 | `συμ` + `βουλίην` · `λοίπης` · `Χαριππον` |

모두 `src:"편찬"` 으로 표시해 `Diorisis` 출처와 패널에서 구분된다.

`ΕΧ` · `ΦΑΙΔ` 는 파이돈 본문에 각각 13 · 15 회 나오는 **화자 약호**로, 어휘가 아니라 대화 표시다. 풀이에 그 사실을 밝혔다.

### 원문 서식 1건

투키디데스 1.5 의 `ἀλλήλους.καὶ` — 마침표 뒤 공백이 빠져 두 단어가 붙어 있었다. 판독이 아니라 서식 문제라 공백을 넣었다. 문장 분할도 이로써 정상화된다.

`συμ βουλίην` (선서) 은 `συμβουλίην` 의 어중 공백 오식으로 보이나 **Diorisis 도 같게 읽어** 교정 근거가 없다. 잠언집의 `ὑπ χωρήσιες` 와 같은 사례로, 두 계통이 공유하는 결함으로 보인다. 대신 두 조각 모두에 표제어 `συμβουλία` 와 사정을 밝힌 풀이를 달았다.

## 3. 한국어 풀이 71.63%

v64 에서 작성한 1,064 개 표제어 풀이가 그대로 재사용되어, 새 항목의 41.5% 가 자동으로 채워졌다. 토큰 기준으로는 71.63% — 빈출어가 대부분 겹치기 때문이다.

100% 로 올리려면 약 600 개 표제어를 더 써야 한다. 별도 라운드로 남긴다.

## 4. 검증

- JS 스캐너 전수 검사 — 미종료 문자열 0 · 문자열 내 개행 0 · 괄호 잔여 0
- **브라우저 실측** — 신규 5편 3,423 토큰 전량 분석 100.00%, 누락 0
- 줄표 수정 실측 — 『변론』에서 병합 토큰 0, `ἀλήθειαν` 개별 조회 가능, 줄표 표시 유지

## 5. 알려진 한계

- **`편찬` 59 항목은 코퍼스 대조로 검증할 수 없다.** 특히 비극·희극의 시어는 문맥 의존이 커서 재검토 가치가 있다.
- **풀이 71.63%.** 나머지 약 600 표제어는 미작성.
- **정역 미제공** — 신규 5편 모두.
- 줄표 수정은 `renderWorkSection` · `renderPassage` 두 곳에만 적용했다. 검색 (`13581` 줄) 은 이미 줄표를 분리자로 다루고 있어 손대지 않았다.

## 6. 파일 변경 요약

| 파일 | Δ |
|---|---|
| `data-morph.js` | +72,172 자 · `MORPH_LOOKUP` 38,572 → 39,256 |
| `index.html` | `APP_VERSION` v65 → v66 · 줄표 토큰화 2곳 |
| `data-works.js` | 투키디데스 1.5 구두점 뒤 공백 1건 |
| `sw.js` | `CACHE_VERSION` v66 → v67 |
| `CHANGELOG_v66.md` | 신규 |

## 7. 다음 세션에서 이어갈 작업

- 신규 5편 한국어 풀이 100% (약 600 표제어)
- 신규 5편 정역
- 커리큘럼 Day 21~60 매핑 (v62 이월)
- 오늘의 단어 카드 (v61 이월)
- Sappho 원문 출처 확보
- 잠언집·신규 5편의 `편찬` 분석과 풀이 전문 검토
