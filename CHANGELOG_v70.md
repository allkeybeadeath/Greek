# v70 — SRS 점검과 어휘 중복 수정

작성: 2026-08-27 · 개인 프로젝트

v69 에서 SRS 를 되살리며 "v44 이후 실제로 동작한 적이 없으니 별도 점검이 필요하다" 고 적어 두었다. 그 점검을 수행했다. **세션 흐름 자체는 온전했고, 대신 어휘 데이터의 중복이라는 별개 결함이 드러났다.**

## 변경 요약

| 지표 | 이전 | 이후 |
|---|---|---|
| SRS 전체 단어 | 1,142 | **800** (고유 표제어) |
| 오늘 due | 58 | **40** (복습 20 + 신규 20) |
| 한 세션 내 중복 카드 | 발생 | **없음** |

버전: `APP_VERSION` v69 → v70 · `CACHE_VERSION` v70 → v71

## 1. SRS 흐름 점검 — 이상 없음

세션을 끝까지 돌려 확인했다.

- 시작 → 카드 표시 (표제어·주요 어미, 정답 숨김)
- `정답 보기` → 뜻·영어·악센트 분석 공개
- 4 단계 평가 (다시 / 어려움 / 보통 / 쉬움) → SM-2 상태 저장
- 첫 정답 채점 후 `{ef: 2.5, interval: 1, reps: 1}` 로 정확히 기록
- 20 장 완주 → `18/20 세션 완료` · XP 54 (정답 18 × 3) 지급
- 통계 화면 — 학습 중 20 · 누적 성공 18 · lapse 2 · 실수율 10% · 14 일 일정 그래프 4 종

**중간에 "저장이 안 된다" 고 보였던 것은 오진이었다.** `saveState()` 가 비동기라 클릭 직후 `localStorage` 를 읽으면 아직 반영 전이다. 1.5 초 뒤 다시 읽으니 20 건과 XP 54 가 모두 정상이었다.

날짜가 08-26 에서 08-27 로 넘어가며 어제 채점한 카드 20 장이 오늘 복습 대상이 된 것도 SM-2 의 정상 동작이다 (interval 1 → 다음 날 due).

## 2. 드러난 결함 — 표제어 중복

점검 중 due 수가 계산과 맞지 않았다. 복습 20 + 신규 상한 20 = **40** 이어야 하는데 화면은 **58** 을 표시했다.

원인은 `ALL_VOCAB` 이다. 네 배열을 단순히 이어 붙인 것이라 같은 표제어가 여러 번 들어 있다.

| | 항목 수 |
|---|---|
| 전체 | 1,142 |
| **고유 표제어** | **800** |
| 중복으로 늘어난 항목 | 342 |

배열 간 겹침은 `TEXTBOOK_W ∩ DCC_W` 만 233 건이고, `ἥλιος` · `γῆ` · `νῦν` · `ἀνά` 는 네 번씩 들어 있다.

`_srsKey(word)` 는 `word.g` 이므로, 학습한 단어 하나가 배열 안의 중복 수만큼 복습 대상으로 잡힌다. 그래서 20 장을 학습했는데 복습이 38 로 부풀었다 (38 + 20 = 58).

**학습자에게 실제로 나타나는 증상은 한 세션에 같은 단어가 여러 번 나오는 것이다.**

### 처리

표제어 단위로 중복을 걷어낸 풀을 만들었다.

```js
function vocabPool(){ /* ALL_VOCAB 을 g 기준 중복 제거, 캐시 */ }
```

먼저 나온 항목을 남긴다. `ALL_VOCAB` 의 이어 붙이는 순서가 교재 → DCC → 일상 → 시민이므로, 교재 단어의 과 정보가 보존된다.

적용 범위는 **표제어 단위로 다뤄야 하는 곳**으로 한정했다.

| 대상 | 이유 |
|---|---|
| `srsDueCards()` | 중복 카드 · due 부풀림 제거 |
| `renderSRS` 의 전체 단어 수 | 1,142 → 800 |
| `renderWordOfDay()` | 중복 표제어가 뽑힐 확률이 최대 4 배 높던 것을 균등화 |
| 홈 오늘의 단어 카드 | 상세 화면과 **같은 풀**을 써야 같은 단어를 가리킨다 |

어휘 목록·퀴즈 등 다른 곳의 `ALL_VOCAB` 사용은 건드리지 않았다. 그쪽의 중복 처리는 성격이 다르므로 별도 판단이 필요하다.

## 3. 검증

- SRS 메인 — `전체 단어 800 · 오늘 due 40 · 학습 중 20`, 홈 카드도 40
- **세션 20 장 전부 고유** — 중복 0
- 오늘의 단어 (`λίμνη`) 가 홈 카드와 상세 화면에서 일치
- 콘솔 오류는 미리보기 브라우저의 서비스 워커 차단 1 종뿐

## 4. 알려진 한계

- **어휘 목록·퀴즈의 중복은 그대로다.** 어휘 탭에서 `γῆ` 를 네 번 만날 수 있다. 이쪽은 출처별로 보여 주는 것이 의도일 수도 있어 손대지 않았다.
- 중복 제거는 **먼저 나온 항목을 남기는** 단순 규칙이다. 뒤쪽 배열의 뜻풀이가 더 나은 경우가 있어도 버려진다.
- `SRS_NEW_PER_DAY = 20` 은 여전히 임의값이며 조절 UI 가 없다.
- 오늘의 단어는 800 개 전체에서 난이도 구분 없이 뽑는다.

## 5. 파일 변경 요약

| 파일 | Δ |
|---|---|
| `index.html` | `vocabPool()` 신설 · `srsDueCards`·`renderSRS`·`renderWordOfDay`·홈 카드가 이를 사용 · `APP_VERSION` v69 → v70 |
| `sw.js` | `CACHE_VERSION` v70 → v71 |
| `CHANGELOG_v70.md` | 신규 |

## 6. 다음 세션에서 이어갈 작업

- 커리큘럼 Day 21~60 매핑 (v62 이월) — 남은 것 중 가장 큼
- 어휘 목록·퀴즈의 중복 처리 방침 결정 (위 §4)
- Sappho 원문 출처 확보
- 기존 사전 항목의 분석 정확도 감사 (v67 §4)
- 정역과 `편찬` 분석, 풀이의 전문 검토
