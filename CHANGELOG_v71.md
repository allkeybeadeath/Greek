# v71 — 펜텔리콘 (Πεντελικόν)

디자인 전면 개편. 요청하신 **투명한 하늘색 · 파랑 · 하양**, 파르테논의 정결한 느낌을
색·활자·부품·읽기 방식의 네 층에 걸쳐 적용했습니다.

이름은 파르테논의 대리석을 캔 산, 펜텔리콘(Πεντελικόν)에서 따왔습니다.

---

## 왜 바꿨나

이전 배색(따뜻한 크림 `#F4E8D8` + 테라코타 `#C44536` + Cormorant Garamond)은
`frontend-design` 스킬이 **"AI 기본값 군집 1번"** 으로 지목하는 조합과 정확히 일치했습니다.
고전을 다루는 앱이 고전처럼 보이지 않고 *고전 테마를 고른 것처럼* 보였습니다.

## 1단계 — 색과 활자

| | 이전 | v71 |
|---|---|---|
| 바탕 | 크림 `#F4E8D8` | 대리석 `#F2F6FA` / 흰 `#FFFFFF` |
| 강조 | 테라코타 `#C44536` | 아이게해 `#14508F` |
| 부드러운 강조 | — | 하늘 `#6FA8D6`, 겹 `rgba(111,168,214,.13)` |
| 글자 | `#1A1612` | 잉크 `#16283A` |
| 괘선 | `#C4986A` | `#CFDCE8` |
| 표제 활자 | Cormorant Garamond | **GFS Didot** (그리스 활자 전통) |
| 한글 | — | **고운돋움 / 고운바탕** |

- 색 토큰을 **의미 이름**으로 다시 세우고(`--marble`, `--sky`, `--aegean`, `--ink`, `--rule`),
  옛 이름은 별칭으로 남겼습니다 (`--terracotta: var(--aegean)`).
  덕분에 본문 **1,287곳의 참조를 한 줄도 고치지 않고** 색이 통째로 바뀝니다.
- 색 리터럴 40종 · 90곳을 직접 치환. 남은 따뜻한 색은 **0개**
  (예외: 초기화 단추의 경고색 `#C0453A` — 의도한 것).
- 신전에 둥근 모서리는 없습니다 → `--r: 0`, `--r-l: 0`.
- 45° 잔무늬 제거. (세로 홈으로 바꿔 봤으나 눈이 어지러워 걷어냈습니다 — 바탕은 평평한 대리석면.)

## 2단계 — 부품

| 부품 | 이전 | v71 |
|---|---|---|
| `.card` | 테두리 + 그림자 + 둥근 모서리 | 흰 석판 · 상단 2px 아이게해 선 · 그림자 없음 |
| `.tile` | 왼쪽 정렬 상자 | **주열(柱列)** — 가운데 정렬, 4열, 상단 아이게해 선 |
| `.stat-cell` | 칸마다 테두리 상자 | 세로 구분선 · `tabular-nums` (숫자 흔들림 없음) |
| `.quote-card` | 검정 대각 그라디언트 | 투명한 하늘색 겹 |
| `.progress` | 5px 막대 | **1px 눈금** |
| `.btn` | 채운 테라코타 | 윤곽 · 위계 3단 (채움 / 윤곽 / 회색윤곽) |
| `.badge` | 원형 메달 | **새긴 네모** |
| `.view-title` | 22px | 30px Didot 아이게해 |

**걷어낸 AI 기본값 (덧셈이 아니라 뺄셈):**

- `border-left: 3px solid` 굵은 색 레일 — **41곳** → 1px 옅은 선
- `linear-gradient(135deg, …)` 대각 그라디언트 — **27곳** → 평평한 색면
- `border-radius: Npx` — **87곳** → `0` (원형 `50%`는 유지)
- `.tiles` 는 `repeat(4, minmax(0,1fr))` — `1fr`의 최소값이 `min-content`라
  긴 한글 라벨이 든 칸만 부풀던 문제를 바로잡았습니다.

## 3단계 — 여백 주석 (scholion)

읽기 화면에서 **모달을 없앴습니다.**

모달은 본문을 덮어 문맥을 끊습니다. 필사본의 방주(scholion)처럼,
짚은 낱말이 있는 **그 행 바로 아래**에 펼칩니다.

- 앞뒤 문장을 보면서 형태분석을 읽을 수 있습니다.
- 같은 낱말을 다시 누르면 접힙니다. 한 번에 하나만 열립니다.
- 짚은 낱말에는 옅은 하늘색 밑줄이 남아 어디를 보고 있는지 잃지 않습니다.
- 표제어 · 파싱 · 한국어 풀이 · 출처(AGDT / PROIEL / Diorisis / 편찬) ·
  본문 검색 · Perseus · Logeion — 모달에 있던 정보는 그대로입니다.
- 형태분석 데이터 지연 로딩도 그 자리에서 처리합니다.
- 적용 화면: 작품 본문(`renderWorkSection`), 발췌 구절(`renderPassage`).
- 모달(`openLookup`)은 검색·어휘 빈도 화면에서 계속 쓰입니다.

---

## 아직 남은 것

- **배경음악** — `harmonia` 시연(아르퀴타스 온음계 · 크플러스-스트롱 현 · 합성 IR 잔향)을
  만들어 두었으나 채택 여부 미정. 현행 12평균율 BGM 그대로입니다.
- 커리큘럼 21~60일차 매핑
- 기본 사전 항목(`src` 없는 것)의 분석 정확도 감사 — 별도 과제

---

# v71.2 ~ v71.4 — 후속

## v71.2 · 흰 바탕 위의 선 제거

본문 위를 지나는 선이 글자와 겹쳐 읽기를 방해한다는 지적을 반영.

- `--marble` `#F2F6FA` → **`#FFFFFF`**
- 세로 홈(fluting) 4곳 제거 — 카드 · 타일 · 바탕(밝은/어두운 모드)
- 그리스 열쇠무늬(meander) 띠 제거 — 본편 + 로딩 화면
- 하단 네비의 세로 그라디언트 → 평면
- 로딩 화면 SVG 안에 숨어 있던 구 테라코타 `#C44536` 제거
  (data URI 내부라 v71 색 치환에서 빠져 있었음). 파일 전체 잔존 **0개**

## v71.3 · 카드 선 제거 + 배경음악 교체

**디자인** — `.card` 상단 2px 선 제거, 여백 24px으로 구분.
`.tile` 은 4칸이 나란히 서 경계가 없으면 뭉치므로 1px 옅은 괘선만 남김.
본문 영역(`.card` · `.passage`) 테두리·무늬 검사 결과 **0개**.

**배경음악** — 12평균율 사인파 → **하르모니아(ἁρμονία)**

| | 이전 | v71.3 |
|---|---|---|
| 음률 | 12평균율 도리아 | 아르퀴타스 온음계 (1/1 28/27 32/27 4/3 3/2 14/9 16/9 2/1) |
| 음색 | 사인 + 삼각파 2배음 | 카플러스-스트롱 현 합성 |
| 공간 | 없음 | 합성 임펄스 응답 컨볼루션 잔향 |
| 리듬 | 480ms 균등 박자 | 그리스 운율 — 장음 2모라에만 음, 단음은 쉼 |

운율은 이암보스 삼보격 · 다크틸로스 육보격 · 아나파이스토스를 21음마다 교대.
공개 API(`start`/`stop`/`isRunning`/`isAvailable`)는 그대로라 호출부 무수정.

### 조율 결함 하나를 찾아 고침

카플러스-스트롱은 지연선 길이가 **정수**여야 하는데 `sr/freq` 는 대개 정수가
아니고, `(x+prev)*0.5` 평균화 필터가 **반 샘플**을 더 지연시킨다. 둘 다 높은
음일수록 비율이 커진다.

`OfflineAudioContext` + 정규화 자기상관(포물선 보간)으로 측정:

| 비 | Hz | 정수 지연 | 소수 지연 보간 |
|---|---|---|---|
| 1/1 | 196.00 | −3.8 센트 | **0.0** |
| 4/3 | 261.33 | −7.7 | **0.0** |
| 3/2 | 294.00 | −5.8 | **0.0** |
| 16/9 | 348.44 | −12.8 | **0.0** |
| 2/1 | 392.00 | −15.3 | **0.0** |

옥타브가 15센트 좁으면 순정률을 쓰는 이유 자체가 무너진다. 소수 지연을
선형 보간으로 채워 8음 전부 오차 0.0센트.

## v71.4 · 커리큘럼 Day 21~60

v62에서 Day 1~20(레슨 3~7)까지만 만들고 멈춰 있던 것을 **60일**로 확장.

- **Day 21~60 = 레슨 8~39**, 어휘 **380개** (내부 중복 0 · 기존 20일과 겹침 0)
- 문법 토픽 **22개** 전부 배치 — `TOPICS` 의 `lesson` 필드로 레슨↔문법을
  맞췄으므로 추측이 아니라 데이터에 근거함
- 하루 어휘 10개 기준, 꼬리가 4개 이하면 앞날에 합쳐 가벼운 날을 만들지 않음
- 그 레슨의 마지막 어휘 날에 문법 토픽을 얹는 기존 패턴 유지
- 그리스어 표제는 밀레토스식 숫자로 연속 (`Λέξεις Θʹ — Μάθημα Ηʹ` …
  `Λέξεις Μηʹ — Μάθημα Λθʹ`)
- `BEGINNER_CURRICULUM_TOTAL_DAYS` 20 → 60

검증: 일차 1~60 연속·결번 없음, 미등록 어휘 0, 미등록 `topicId` 0,
배열 길이 == 상수. 브라우저에서 Day 21 · 33 · 45 · 60 을 열어 어휘 카드 ·
문법 표 · 어휘 시험 세 종류가 모두 그려지는 것과 미도달 일차 잠금(🔒) 확인.

`lesson:99` 인 종합 토픽 7개(`men-de` · `aor-impf` · `cond` · `opt` ·
`mi-verbs` · `acc-abs` · `syn-meta`)와 레슨 40 어휘는 Day 61+ 몫으로 남김.
